# v0.6.1 - Root Route Convention Cleanup

- Root pages now use `app/routes/page.html` and `app/routes/page.ts`.
- `app/routes/home` now maps to `/home`.
- Added migration diagnostics, updated templates and root-route reload coverage.
