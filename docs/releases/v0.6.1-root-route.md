# v0.6.1 - Root Route Convention Cleanup

The root route now lives at `app/routes/page.html`, with `app/routes/page.ts` as its optional loader. The old `app/routes/home` mapping was removed; that folder now maps to `/home`.

See [the migration guide](../migration-v0.6.1.md) for the safe manual move.
