# Kumquat.ts Docs

Kumquat.ts is an experimental HTML-first fullstack framework for TypeScript.

Start with `getting-started.md`, then read routing, pages, layouts, API, actions, session, auth, forms, config, node runtime, and deploy notes.
